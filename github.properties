description=Shared utility library for Blizzity projects — argument parsing, password hashing, key generation, and math helpers.
example=```|Arguments args = new Arguments(new String[]{"--debug", "--port=8080"});|String port = args.get("port", "3000");|boolean debug = args.getAsBoolean("debug");||String hashed = PasswordUtils.hashPassword("secret");|boolean valid = PasswordUtils.checkPassword("secret", hashed);||String apiKey = KeyUtils.generateApiKey();|```||Dependency: `githubImplementation "Blizzity:common:LATEST"`
